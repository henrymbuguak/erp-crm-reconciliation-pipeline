"""Marks `eval/` as an importable package for `tests/test_score.py`.

Not part of the `pipeline`/`datagen` src-layout packages: this lives at the
repo root deliberately, per CLAUDE.md's ground_truth.json eval-only rule.
"""
